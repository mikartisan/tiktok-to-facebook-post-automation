name: TikTok to Facebook Auto Upload

on:
    schedule:
        - cron: "0 10 * * *"  # 6:00 PM PH
        - cron: "0 12 * * *"  # 8:00 PM PH
        - cron: "0 14 * * *"  # 10:00 PM PH
        - cron: "0 16 * * *"  # 12:00 AM PH
        - cron: "0 17 * * *"  # 1:00 AM PH
        - cron: "0 0 * * *"   # 8:00 AM PH
    workflow_dispatch: # manual trigger

jobs:
    repost:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repo
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: "3.10"

            - name: Install dependencies
              run: |
                  python -m pip install --upgrade pip
                  pip install requests yt-dlp

            - name: Run TikTok → Facebook script
              env:
                  PAGE_ACCESS_TOKEN: ${{ secrets.PAGE_ACCESS_TOKEN }}
                  PAGE_ID: ${{ secrets.PAGE_ID }}
              run: python tiktok.py

            # Commit updated last_video_id.txt if changed
            - name: Configure Git for commit
              run: |
                  git config --global user.name "github-actions[bot]"
                  git config --global user.email "github-actions[bot]@users.noreply.github.com"

            - name: Commit updated last_video_id.txt
              run: |
                  git add downloads/last_video_id.txt
                  git diff --cached --quiet || git commit -m "Update last_video_id after new TikTok upload"

            - name: Push changes
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: git push origin HEAD:${{ github.ref_name }}
